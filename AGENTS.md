# opc-workspace AI 开发约定

本文件适用于仓库内所有目录。AI 在本仓库中进行分析、实现、修复、测试和文档维护时，必须遵守以下约定。

## 仓库与操作边界

- 开始工作前先核对 Git 根目录、当前分支和工作树；保留用户已有修改，不覆盖、不清理、不提交无关文件。
- 除非用户当前明确授权，否则不要 commit、push、部署或修改远程仓库。
- 本地开发与桌面运行不使用 Docker。遵守 PRD 当前版本边界，不把 AI、知识库或其他后续版本能力提前混入当前版本。

## 文档是功能交付的一部分

任何新增功能、行为变化、数据结构调整、API 变更、模块联动或版本范围变化，都必须在同一次工作中同步更新相关文档。功能代码完成但文档仍描述旧行为，视为任务未完成。

开始实现前，先阅读与任务相关的文档：

1. `docs/opc-workspace-PRD.md`：产品范围、版本边界和实现追踪。
2. `docs/functional-architecture.md`：跨模块关系、事件和事实归属。
3. `docs/modules/<module>.md`：模块流程、数据、API、依赖和验收标准。
4. `docs/README.md` 与 `docs/modules/README.md`：文档索引和状态总览。
5. 根目录 `README.md`：开发、构建、数据目录和当前完成范围。

## 每次功能开发必须检查的文档更新

根据实际变更更新以下内容，不要机械复制或虚构完成状态：

- **模块文档**：更新当前实现状态、用户流程、交互行为、数据字段、API、状态机、依赖、错误处理、实施阶段、验收标准和代码证据。
- **PRD**：当产品范围、版本归属、功能状态、业务规则、数据/API 契约或验收口径变化时同步更新。
- **整体功能架构**：当模块之间的输入输出、事件流、事实归属、调用关系或安全边界变化时同步更新。
- **文档索引**：新增、重命名、拆分或删除模块文档时，更新 `docs/README.md` 和 `docs/modules/README.md`。
- **根 README**：依赖、启动命令、构建方式、环境变量、端口、数据目录、开发脚本或当前交付范围变化时同步更新。
- **迁移与接口说明**：SQLite schema、迁移入口或版本化 API 变化时，记录迁移版本、兼容策略、失败恢复和验证方法。

如果改动只是无行为变化的内部重构、格式化或测试补强，可以不修改产品文档，但最终汇报必须明确说明为什么无需更新文档。

## 文档写作规则

- 明确区分“当前已实现”“部分完成”“页面骨架”“未开始”和“后续规划”。
- 当前状态必须以代码、迁移、测试和可运行结果为依据；只有 UI、占位按钮、静态数据或预留表时，不得写成已完成。
- 规划内容必须标注目标版本，不得把未来设计写成当前能力。
- 文档中的字段名、路由、API、状态值、版本号、端口和文件路径必须与代码一致。
- 删除或替换功能时，同时删除过期流程、截图引用、接口说明和验收条件，避免保留互相冲突的描述。
- 优先修改现有权威文档，不为同一主题创建重复说明。

## 推荐开发流程

1. 核对仓库、Git 状态和相关文档。
2. 明确当前实现事实、目标版本和受影响模块。
3. 实现最小完整纵向切片，包括加载、空状态和错误处理。
4. 运行与风险相称的格式化、类型检查、测试、构建和桌面检查。
5. 根据最终实现同步更新文档，不提前按计划稿宣称完成。
6. 使用 `git diff` 同时审查代码和文档，确认二者一致。
7. 最终汇报分别说明代码完成内容、文档更新位置、验证结果、Git 状态和剩余限制。

## 完成标准

功能任务只有同时满足以下条件才算完成：

- 实现与用户要求及当前版本边界一致。
- 相关测试或检查通过，失败项已说明原因。
- 对应模块文档反映真实实现状态。
- 必要的 PRD、架构、索引和 README 已同步。
- 没有把占位能力或未来规划误标为已交付。
- 未擅自提交、推送或部署。
