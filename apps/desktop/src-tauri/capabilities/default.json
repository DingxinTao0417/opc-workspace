{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core capability for the trusted main webview only; browser children have no IPC permissions",
  "webviews": ["main"],
  "permissions": ["core:default"]
}
